
============
Installation
============

This package is freely available on `PyPi <add link>`_, to install it on your working
environment simply run the command below on your terminal using pip:

.. code-block:: 

   $ pip install saru

This will automatically install the software with all the dependencies required.